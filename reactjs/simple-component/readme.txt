Example for very simple reactjs example. It can be taken as a hello world example.

It creates a component which creates an element in virtual dom using React.createClass.

Later the virtual dom is bound to real dom using React.render
